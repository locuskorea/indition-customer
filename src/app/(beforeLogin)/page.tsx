import Buttons from "../_components/Buttons";

export default function BeforeLoginMain() {
  return (
    <div>
      로그인 전 페이지
      <Buttons text={"버튼"} img={"/file.svg"} size={"small"} color={"blue"} />
    </div>
  );
}
